---
description: Diagnose why a GitLab merge request is failing
---

Use the `glab` CLI to investigate why my merge request is failing. Check the MR's pipeline status, failed jobs, and job logs to determine the root cause. $ARGUMENTS
